@page "/herencia"
@using EjemploIntermedios.Components.Clases

<h1 class="text-center mt-4">Herencia</h1>

<div class="container mt-4">
    <div class="row mb-3">
        <div class="col">
            <button class="btn btn-primary w-100" @onclick="MostrarMensajes">Mostrar Mensajes</button>
        </div>
    </div>

    <div class="row mb-3">
        <div class="col">
            <p role="status" class="alert alert-info text-center">@MensajePerro</p>
        </div>
    </div>
    <div class="row mb-3">
        <div class="col">
            <p role="status" class="alert alert-info text-center">@MensajeGato</p>
        </div>
    </div>
</div>

@code {

    private string MensajePerro = "";
    private string MensajeGato = "";

    private void MostrarMensajes()
    {

        var perro = new Perro();
        var gato = new Gato();

        MensajePerro += perro.Caminar();
        MensajePerro += perro.Ladrar();

        MensajeGato += gato.Caminar();
        MensajeGato += gato.Maullar();
    }
}
